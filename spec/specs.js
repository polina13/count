describe('count',function(){
   it("will return true, if output of number reaches 30", function() {
     expect(count(30)).to.equal(true);
   });










  // it("will return false for a number greater than 30",function(){
  //     expect(count(30)).to.equal(false);
  // });


});
